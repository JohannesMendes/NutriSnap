{
  "name": "nutrisnap-functions",
  "description": "Backend seguro do NutriSnap: guarda a chave da API do Gemini no servidor (via Firebase Secret Manager) e nunca a expõe pro app.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "logs": "firebase functions:log",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^6.0.1",
    "firebase-admin": "^12.6.0"
  },
  "private": true
}
